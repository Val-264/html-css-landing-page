# Tarea: html + css landing page

## Instrucciones para ejecutar:
1. Clone el repositorio:
2. Instale dependencias:
   `npm install`
3. Inicie el servidor:
   `npm start`
4. Abra la página en el navegador:
   `http://localhost:3000`